
import { css } from 'styled-components';
import { GRAY } from '../colors/Colors';

export const iconBaseStyle = css`
  height: 25px;
  align-self: center;
  color: ${ GRAY };
`;

export default {
  iconBaseStyle,
}